Settings Protocols
====================

When working with settings, static values are sometimes not flexible enough. gordon provides severals protocol which will allow you to
customize your settings.

It is important to understand the difference between :doc:`settings_protocols` and :doc:`parameters_protocols`. Settings protocols are evaluated on ``build`` time, and
parameter_protocols on ``apply``.


Reference ``ref://``
---------------------

You can use references ...
